<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:fitsSystemWindows="true"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ImageView
        android:src="@drawable/bg_home"
        android:scaleType="centerCrop"
        android:contentDescription="@string/app_name"
        android:layout_width="match_parent" android:layout_height="match_parent"/>

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:backgroundTint="@android:color/transparent"
        app:titleCentered="true"
        app:title="@string/app_name"
        app:menu="@menu/menu_home">

        <!-- left account icon as nav button -->
    </com.google.android.material.appbar.MaterialToolbar>

    <!-- Fake left icon using container under toolbar -->
    <LinearLayout
        android:id="@+id/accountBtn"
        android:layout_width="48dp" android:layout_height="48dp"
        android:layout_marginStart="12dp" android:layout_marginTop="12dp"
        android:background="@drawable/rounded_container"
        android:gravity="center"
        app:layout_anchor="@id/topAppBar"
        app:layout_anchorGravity="start|top">
        <ImageView
            android:src="@android:drawable/ic_menu_manage"
            android:layout_width="24dp" android:layout_height="24dp"/>
    </LinearLayout>

    <ScrollView
        android:layout_marginTop="72dp"
        android:layout_width="match_parent" android:layout_height="match_parent">
        <LinearLayout
            android:orientation="vertical" android:padding="16dp"
            android:layout_width="match_parent" android:layout_height="wrap_content">

            <Button android:id="@+id/cardEmotion" android:text="🎨 Emotion → Art" style="@style/HomeCard"/>
            <Button android:id="@+id/cardWhatIf"  android:text="🌀 What-If Simulator" style="@style/HomeCard"/>
            <Button android:id="@+id/cardStory"   android:text="📖 Peer Story" style="@style/HomeCard"/>
            <Button android:id="@+id/cardMusic"   android:text="🎵 Mood Music" style="@style/HomeCard"/>
            <Button android:id="@+id/cardVR"      android:text="🌍 VR World" style="@style/HomeCard"/>

            <Button
                android:id="@+id/btnUsageAccess"
                android:text="Enable Mindful Alerts (Usage Access)"
                android:layout_marginTop="12dp"
                android:backgroundTint="@color/purple_500"
                android:textColor="@android:color/white"
                android:layout_width="match_parent" android:layout_height="wrap_content"/>

            <Button
                android:id="@+id/btnMindfulCoach"
                android:text="🧘 Mindful Coach"
                android:layout_marginTop="8dp"
                android:backgroundTint="@color/teal_700"
                android:textColor="@android:color/white"
                android:layout_width="match_parent" android:layout_height="wrap_content"/>

        </LinearLayout>
    </ScrollView>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabChat"
        android:contentDescription="Chatbot"
        app:srcCompat="@android:drawable/ic_btn_speak_now"
        app:backgroundTint="@color/teal_700"
        app:tint="@android:color/white"
        app:layout_anchorGravity="bottom|end"
        android:layout_margin="16dp"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        app:layout_anchor="@id/topAppBar"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
