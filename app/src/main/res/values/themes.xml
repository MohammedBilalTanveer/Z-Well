<resources xmlns:tools="http://schemas.android.com/tools">
    <!--suppress AndroidDomInspection -->
    <style name="Theme.ZWell." parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@color/bg_dim</item>
        <item name="android:navigationBarColor">@color/bg_dim</item>
        <item name="colorSurface">@color/bg_dim</item>
        <item name="android:textColor">@color/on_bg_dim</item>
    </style>

    <style name="Theme.ZWell..Dialog" parent="Theme.Material3.Dark.Dialog"/>
</resources>
