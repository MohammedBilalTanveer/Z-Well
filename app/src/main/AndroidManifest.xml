<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZWell">

        <!-- Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".AccountActivity" />

        <!-- Home -->
        <activity android:name=".HomeActivity" />

        <!-- Features -->
        <activity android:name=".WhatIfActivity" />
        <activity android:name=".StoryActivity" />
        <activity android:name=".MusicActivity" />
        <activity android:name=".VRWorldActivity" />
        <activity android:name=".EmotionToArtActivity" />

        <!-- Mindful Coach Dialog -->
        <activity
            android:name=".MindfulDialogActivity"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />

        <!-- Background Service -->
        <service
            android:name=".UsageStatsService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.PACKAGE_USAGE_STATS" />
    </application>

</manifest>
